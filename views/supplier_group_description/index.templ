package supplier_group_description

import (
	"fmt"
	"strings"

	"bajscheme/models"
	"bajscheme/views/layout"
)

templ SupplierGroupDescriptionList(titlePage string,  supplier_group_descriptions []models.SupplierGroupDescription) {
	<div class="flex justify-between max-w-2xl mx-auto border-b border-b-slate-600 mb-8 pb-2">
		<h1 class="text-2xl font-bold text-center">
			{ strings.Trim(titlePage, "| ") }
		</h1>
		<a hx-swap="transition:true" class="btn btn-info p-4 hover:scale-[1.1]" href="/supplier_group_description/create">
			New
		</a>
	</div>
	<section class="overflow-auto max-w-2xl max-h-96 mx-auto bg-slate-600 rounded-lg shadow-xl">
		<table class="table table-zebra">
			<!-- head -->
			<thead class="bg-slate-700">
				<tr>
					<th></th>
					<th class="text-center">Group Id</th>
<th class="text-center">Language</th>
<th class="text-center">Title</th>

					<th class="text-center">Options</th>
				</tr>
			</thead>
			if len(supplier_group_descriptions) != 0 {
				<tbody>
					for _, supplier_group_description := range supplier_group_descriptions {
						<tr>

                            <td>{fmt.Sprintf("%s",supplier_group_description.GroupID)} </td>\n<td>{fmt.Sprintf("%s",supplier_group_description.Language)} </td>\n<td>{fmt.Sprintf("%s",supplier_group_description.Title)} </td>\n
							<td class="flex justify-center gap-2">
								<a
 									hx-swap="transition:true"
 									href={ templ.URL(fmt.Sprintf("/supplier_group_description/edit/%d", supplier_group_description.GroupID)) }
 									class="btn btn-primary p-3 hover:scale-[1.1]"
								>
									Edit
								</a>
								<button
 									hx-swap="transition:true"
 									hx-delete={ fmt.Sprintf("/supplier_group_description/delete/%d", supplier_group_description.GroupID) }
 									hx-confirm={ fmt.Sprintf("Are you sure you want to delete the supplier_group_description with ID #%d?", supplier_group_description.GroupID) }
 									hx-target="body"
 									class="btn btn-error p-3 hover:scale-[1.1]"
								>
									Delete
								</button>
							</td>
						</tr>
					}
				</tbody>
			} else {
				<tbody>
					<tr>
						<td colspan="4" align="center">
							supplier_group_description is empty
						</td>
					</tr>
				</tbody>
			}
		</table>
	</section>
}

templ SupplierGroupDescriptionIndex(
    title,
    username string,
    fromProtected bool,
    isError bool,
	errMsgs, sucMsgs []string,
    cmp templ.Component,
    ) {
	@layout.Base(title, username, fromProtected, isError, errMsgs, sucMsgs) {
		@cmp
	}
}
